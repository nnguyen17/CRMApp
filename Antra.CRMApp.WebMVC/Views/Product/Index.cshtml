@model IEnumerable<ProductResponseModel>

<a asp-action="Create" asp-controller="Product" class="btn bg-success" >Add Product</a>
<div class="container">
    <div class="row">
        @foreach(var item in Model)
        {
            <div class="col-3">
            <div class="card" style="width: 18rem;">
                <div class="card-body">
                    <h5 class="card-title">@item.Name</h5>
                    <p class="card-text">@item.UnitPrice</p>
                    <a asp-action="Edit" asp-controller="Product" asp-route-id="@item.Id" class="btn btn-primary">Edit</a>
                    <a class="btn bg-danger" asp-action="Delete" asp-controller="Product" asp-route-id="@item.Id">Delete</a>
                </div>
            </div>
            </div>
        }
    </div>
</div>
